import React, {useState, useEffect} from 'react';

function Shop() {

    const fetchItems= async () => {
        const data = await fetch('')
    }
        return (
            <div>
                <h2>Shop Info</h2>
            </div>
        );
}

export default Shop;